# SDPE-CDS-001 §3.4
apiVersion: v1
kind: Service
metadata:
  name: csc06-pipeline-workflow
  labels:
    app.kubernetes.io/name: csc06-pipeline-workflow
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: csc06-pipeline-workflow
  ports:
    - port: 80
      targetPort: 3000
      name: http
