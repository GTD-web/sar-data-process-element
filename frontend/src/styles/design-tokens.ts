/**
 * SDPE Design Tokens — n8n dark + emerald green accent
 *
 * 모든 색상은 이 파일과 globals.css의 CSS 변수에서 관리합니다.
 * - CSS 변수(Tailwind): `text-accent`, `bg-card`, `border-border` 등
 * - 인라인 스타일(그래프): 이 파일의 hex 값 사용
 */

// ── Surfaces ──
export const bg = '#1a1a1a';
export const surface = '#262626';
export const surfaceRaised = '#333333';

// ── Borders ──
export const border = '#3a3a3a';
export const borderStrong = '#4a4a4a';

// ── Text ──
export const textPrimary = '#f5f5f5';
export const textSecondary = '#a0a0a0';
export const textMuted = '#6b6b6b';

// ── Accent (emerald) ──
export const accent = '#34d399';

// ── Semantic ──
export const success = '#34d399';
export const destructive = '#ef4444';
export const warning = '#fbbf24';

// ── Graph-specific ──
export const edge = '#2a8a6a';
export const edgeSuccess = '#34d399';
export const edgeActive = '#34d399';
export const edgeMuted = 'var(--graph-edge-muted)';
export const canvasDot = '#2a2a2a';
export const nodeDefault = '#4a4a4a';
